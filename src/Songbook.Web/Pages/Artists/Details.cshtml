@page "{id:int}"
@model Songbook.Web.Pages.Artists.DetailsModel

<h1 class="mb-3">@Model.Artist.Name</h1>

@if (Model.Artist.Songs.Any())
{
    <h5>Песни</h5>
    <ul class="list-unstyled ms-3">
        @foreach (var song in Model.Artist.Songs)
        {
            <li>
                • <a asp-page="/Songs/Details"
                     asp-route-id="@song.Id"
                     class="text-decoration-none">
                    @song.Title
                </a>
            </li>
        }
    </ul>
}
else
{
    <p class="text-muted">У этого артиста пока нет песен.</p>
}

<div class="mt-4 d-flex gap-2">
    <a asp-page="/Songs/Create"
       asp-route-artistId="@Model.Artist.Id"
       class="btn btn-primary">
        ➕ Добавить песню
    </a>

    <a asp-page="Edit"
       asp-route-id="@Model.Artist.Id"
       class="btn btn-outline-secondary">
        ✏ Редактировать артиста
    </a>

    <a asp-page="Delete"
       asp-route-id="@Model.Artist.Id"
       class="btn btn-outline-danger">
        🗑 Удалить артиста
    </a>

    <a asp-page="Index"
       class="btn btn-secondary ms-auto">
        ← К списку артистов
    </a>
</div>
